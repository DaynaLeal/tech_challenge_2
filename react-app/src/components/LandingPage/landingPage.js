import React from "./node_modules/react";
import "./style.css";

const LandingPage = (props) => {
  return (
    <div>
      <h1>Testing: This should render if App and LandingPage components are working</h1>
    </div>
  );
};

export default LandingPage;
